name: Go dispatch

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        required: true
        type: string
env:
  ENV_TEST: ${{vars.VAR_TEST01}}

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: 1.23

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...

    - name: Test echo
      run: echo "Deploying to ${{ inputs.environment }} ${{secrets.GITHUB_TOKEN}}"

    - name: test env
      run: echo "Env test var ${{vars.VAR_TEST01}} env $ENV_TEST"
